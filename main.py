from figuras import Figura
from cuadrado import Cuadrado
from triangulo import Triangulo
from circulo import Circulo

c = Cuadrado(5)
t = Triangulo(6)
ci = Circulo(4)

print(c.mostrar_info())
print(t.mostrar_info())
print(ci.mostrar_info())